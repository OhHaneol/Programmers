class Solution {
    public double solution(int[] arr) {
        double answer = 0;
        int sum = 0;
        
        for(int c : arr) {
            sum += c;
        }
        
        answer = (double) sum / arr.length;
        
        return answer;
    }
}

